services:
  - type: web
    name: client
    env: node
    rootDirectory: client
    buildCommand: ./render-build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    runtime: node 18