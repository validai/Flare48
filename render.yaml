services:
  - type: web
    name: client
    env: node
    rootDirectory: client
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist
    envVars:
      - key: NODE_ENV
        value: production
    runtime: node 18